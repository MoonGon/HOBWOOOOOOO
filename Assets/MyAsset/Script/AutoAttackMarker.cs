using UnityEngine;

/// <summary>
/// Marker component: ใส่บน GameObject ที่ต้องการให้โจมตีอัตโนมัติเมื่อถึงตา (เช่น Hunter)
/// ไม่ต้องมีโค้ดเพิ่มเติม — ใช้เป็น flag เท่านั้น
/// </summary>
public class AutoAttackMarker : MonoBehaviour
{
    // intentionally empty - marker only
}